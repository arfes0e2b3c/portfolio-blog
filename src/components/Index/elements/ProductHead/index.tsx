import { productHead, productHeadTitle } from './styles/productHead.css'

export const ProductHead = () => {
	return (
		<section className={productHead}>
			<h2 className={productHeadTitle}>PRODUCTS</h2>
		</section>
	)
}
